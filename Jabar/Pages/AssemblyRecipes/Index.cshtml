@page
@model Jabar.Pages.AssemblyRecipes.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AssemblyRecipe[0].Item)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.AssemblyRecipe) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Item.ItemId)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.AssemblyRecipeId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.AssemblyRecipeId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.AssemblyRecipeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
