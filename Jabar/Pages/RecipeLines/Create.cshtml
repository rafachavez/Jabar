@page
@model Jabar.Pages.RecipeLines.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>


<h4>RecipeLine</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">

            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="RecipeLine.ItemId" class="control-label"></label>
                <select asp-for="RecipeLine.ItemId" class="form-control" asp-items="ViewBag.ItemId"></select>
            </div>
            <script>
                $(document).ready(function () {
                    $('.js-example-basic-single').select2();
                });
            </script>
            @*<div class="form-group">
                    <label asp-for="RecipeLine.AssemblyRecipeId" class="control-label"></label>
                    <select asp-for="RecipeLine.AssemblyRecipeId" class="form-control" asp-items="ViewBag.AssemblyRecipeId"></select>
                </div>*@
            <div class="form-group">
                <label asp-for="RecipeLine.RequiredItemQty" class="control-label"></label>
                <input asp-for="RecipeLine.RequiredItemQty" class="form-control" />
                <span asp-validation-for="RecipeLine.RequiredItemQty" class="text-danger"></span>
            </div>
            @*<div class="form-group">
                    <label asp-for="RecipeLine.LastModifiedBy" class="control-label"></label>
                    <input asp-for="RecipeLine.LastModifiedBy" class="form-control" />
                    <span asp-validation-for="RecipeLine.LastModifiedBy" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="RecipeLine.LastModifiedDate" class="control-label"></label>
                    <input asp-for="RecipeLine.LastModifiedDate" class="form-control" />
                    <span asp-validation-for="RecipeLine.LastModifiedDate" class="text-danger"></span>
                </div>*@
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-outline-dark" />
            </div>
        </form>
    </div>
</div>

<div>
    @*<a asp-page="./Details" asp-route-id="@Model.RecipeLine.ItemId">Details</a>*@

    <a asp-page="/AssemblyRecipes/Details" asp-route-id="@Model.AssemblyRecipe.AssemblyRecipeId" class="btn btn-outline-dark">Cancel - Back to Recipe</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    $(document).ready(function() {
    $('.js-example-basic-single').select2();
    });
}
